meta {
  name: Login
  type: http
  seq: 14
}

post {
  url: http://localhost:8000/api/login
  body: formUrlEncoded
  auth: inherit
}

body:form-urlencoded {
  email: {{Email}}
  password: {{password}}
}

settings {
  encodeUrl: true
  timeout: 0
}
